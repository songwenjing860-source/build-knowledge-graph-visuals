---
name: build-knowledge-graph-visuals
description: 从中文课程文章、技术专栏、访谈材料、文档或章节内容中抽取知识实体与有方向的关系，构建可编辑的竖版知识图谱，并输出同构的黑底、白底 SVG 与高清 PNG/JPG。用于用户要求“整理知识图谱、网状关系图、章节复习大图、手机传播长图、黑白双主题、高清知识图谱”或希望复用中心辐射式网状知识图谱画风的任务；不用于只有先后顺序的普通流程图、没有关系语义的思维导图或数据统计图。
---

# 内容驱动的知识图谱视觉化

## 核心定位

把课程内容转换为“可验证的知识关系 + 可传播的视觉图谱”。先证明图谱成立，再追求画面漂亮。

合格的图谱必须同时满足：

- 节点是知识对象，不是文章目录的机械搬运。
- 连线有明确动词语义，不用装饰性连线伪造“网状感”。
- 关键关系能回到原文证据，推断关系必须标明。
- 黑底和白底共用一套节点、坐标与关系，只切换主题变量。
- SVG 是母版；PNG/JPG 是交付格式。文字密集图禁止依赖生图模型重绘。

## 先读这些参考

按任务需要读取，避免一次加载全部内容：

- `references/modeling-method.md`：知识抽取、去重、关系建模和图谱规格。开始建模时必读。
- `references/theme-system.md`：竖版布局、手机直读与深度海报两档密度、黑白主题变量。开始绘图时必读。
- `references/adversarial-review.md`：第一性原理和对抗式审查。交付前必读并逐项执行。
- `references/qa-checklist.md`：渲染后的内容、文字、越界、对比度与导出检查。交付前必读。
- `assets/examples/`：黑底、白底示例，只用于校准视觉语言。不要照抄示例内容或节点位置。

## 工作流

### 1. 明确读者任务

先回答三个问题：

1. 读者看完图要能回答什么问题？
2. 这张图是课前导航、课后复习，还是完整知识索引？
3. 图是在手机信息流中直接阅读，还是允许点开缩放？

第三个问题决定密度：

- `mobile`：无需缩放阅读，默认优先。节点少、字号大。
- `poster`：完整大图，允许点开缩放。信息更全，但不能宣称“手机直读”。

用户未指定时，先做 `mobile`；只有明确要求“一张大图包含尽可能多的知识”时才用 `poster`。

### 2. 消化全部原文

完整读取用户指定的文章或章节，不只看标题和小结。对 DOCX 使用文档读取能力提取正文；对多篇文章建立来源编号。

先写一句中心判断，再抽取：

- 核心能力或核心对象
- 输入、前提与约束
- 关键模块或机制
- 常见问题与根因
- 优化动作
- 衡量指标
- 面试表达或实践产出

不要平均分配节点。按对读者目标的贡献筛选。

### 3. 建立图谱规格

绘图前先形成节点表和关系表。使用 `references/modeling-method.md` 中的字段与关系词库。

最低要求：

- 每个节点有唯一 ID、类型、短标签、原文证据和优先级。
- 每条边有起点、关系动词、终点、强弱和证据。
- 把每条边读成“起点 + 关系 + 终点”的完整句子；读不通就修改或删除。
- 不用“相关”“联系”“影响”这类含混关系掩盖推理缺口。

### 4. 通过第一性原理闸门

在排版前检查：

- 如果去掉颜色和圆环，这组节点与关系仍然成立吗？
- 中心节点真的是路由中枢，还是仅仅是课程标题？
- 哪些关系来自原文，哪些只是编辑推断？
- 如果只保留 20% 的节点，哪些仍足以解释 80% 的章节价值？
- 这份内容是否其实更适合流程图、层级图或表格？

如果没有三条以上有意义的交叉关系，不要硬做知识图谱。改用更诚实的图形结构。

### 5. 选择布局

默认使用“中心 + 第一圈关键模块 + 外圈问题/机制/指标/表达”的竖版网状布局。

布局规则：

- 中心：1 个核心能力或核心判断。
- 第一圈：4-6 个关键模块，承担主叙事。
- 外圈：按类型组织问题、机制、指标和实践表达。
- 强关系：实线带箭头，并显示动词标签。
- 弱关联：虚线，不加箭头或使用更低对比度。
- 重要节点靠近中心；低优先级节点靠外或删除。
- 尽量让边从节点边缘出发，避免穿过文字和其他节点。

需要纯流程、严格层级或时间顺序时，不套用网状布局。

### 6. 绘制共享 SVG 母版

使用 SVG、HTML/SVG 或其他确定性矢量方式绘制。默认字体栈：

```css
-apple-system, BlinkMacSystemFont, "PingFang SC", "Noto Sans CJK SC", "Microsoft YaHei", sans-serif
```

要求：

- 所有文字必须来自图谱规格，禁止生图模型重新拼写中文。
- 黑白两版共用几何结构；只替换背景、文字、连线、标签底色与阴影变量。
- 为标题、中心节点、一级节点、外圈节点和边标签建立稳定字号层级。
- 卡片内文字必须有明确安全边距，不靠自动换行碰运气。
- 保留可编辑 SVG，不把 PNG 当源文件继续修改。

主题变量和字号下限见 `references/theme-system.md`。

### 7. 同时生成黑底和白底

默认交付两套主题：

- `*-dark.svg`：深色课程海报、演示和深色页面。
- `*-light.svg`：白色课程正文、打印和浅色页面。

节点、关系、文案和坐标必须完全一致。任何内容差异都视为版本漂移，必须修正。

生成后先比较两份 SVG 的可见结构。执行脚本时始终从 Skill 目录解析脚本路径，不要假设当前工作目录就是 Skill：

```bash
python <skill-dir>/scripts/compare_svg_structure.py topic-dark.svg topic-light.svg
```

### 8. 高清渲染

使用随附脚本把 SVG 渲染为 PNG，并可选导出 JPG：

```bash
python <skill-dir>/scripts/render_svg.py topic-dark.svg --out topic-dark.png --scale 2 --jpg
python <skill-dir>/scripts/render_svg.py topic-light.svg --out topic-light.png --scale 2 --jpg
```

脚本会读取 SVG 尺寸、调用本机 Chromium/Chrome、验证最终像素尺寸，并在 macOS 上用高质量参数输出 JPG。

### 9. 对抗式审查

完整执行 `references/adversarial-review.md`，至少做四类攻击：

- 内容真实性攻击
- 关系必要性攻击
- 替代结构攻击
- 手机可读性与视觉遮挡攻击

不要自己宣布“已检查”就结束。记录发现的问题和实际修正；没有修正时说明为什么通过。

### 10. 视觉验收

同时查看整图和关键局部。执行 `references/qa-checklist.md`。

至少确认：

- 没有文字越界、截断、遮挡或重叠。
- 箭头没有压住节点文字。
- 黑底和白底的关键关系都清楚。
- 最小字号符合所选密度档。
- 导出尺寸与预期一致。
- 在目标显示宽度下实际预览，而不是只看原始大图。

## 交付文件

默认交付：

```text
<topic>-graph-spec.md
<topic>-knowledge-graph-dark.svg
<topic>-knowledge-graph-dark.png
<topic>-knowledge-graph-light.svg
<topic>-knowledge-graph-light.png
```

用户需要 JPG 时再增加 JPG，不用 JPG 替代 SVG 或 PNG 母版。

交付说明只写：采用的密度档、文件路径、像素尺寸、主要审查修正和仍存在的限制。不要用“极致”“完美”等无法验证的词。

## 失败条件

出现以下任一情况，不得按完成交付：

- 只把文章目录放进不同颜色的框里。
- 大量连线没有关系标签或语义不清。
- 图谱包含原文没有支持的因果关系，却未标注为推断。
- 黑白两版节点或文字不一致。
- 小字只能在原始像素下看清，却声称手机无需缩放。
- 发现遮挡、越界或错字后仍以“整体效果不错”为理由放行。
